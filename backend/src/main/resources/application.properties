spring.application.name=alumni-network
server.port=8080

# PostgreSQL Configuration
# spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/alumnidb}
# spring.datasource.username=${DATABASE_USERNAME:postgres}
# spring.datasource.password=${DATABASE_PASSWORD:password}
# spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Configuration
spring.datasource.url=jdbc:h2:mem:alumnidb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Secret (Reads from environment variable in production)
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expirationMs=86400000
