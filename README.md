# AlumniNexus

A React + Vite + Tailwind project (converted from TanStack Start to plain npm/Vite + react-router-dom).

## Setup

```bash
npm install
npm run dev
```

Open http://localhost:5173

## Build

```bash
npm run build
npm run preview
```
